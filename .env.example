# Supabase (plan free) — Project Settings > API
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key

# Generar con: openssl rand -base64 32
ADMIN_SESSION_SECRET=

# Resend (plan free: 3.000 mails/mes, 100/día) — resend.com/api-keys
RESEND_API_KEY=
# Mail verificado en Resend desde el que se manda todo
RESEND_FROM_EMAIL="nmtech solutions <hola@tu-dominio.com>"

# El mail de contacto público de /contacto ya NO se configura acá: se edita
# desde /admin → Configuración → Contacto.
